#!/usr/bin/env python3
"""Wrapper so `scripts/dsl-debug` works without pip install."""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent / "src"))
from dsl_debug.cli import cli

cli()
